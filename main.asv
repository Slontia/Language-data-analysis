
% popData = xlsread("data.xlsx", "pop");
% dataSize = size(popData, 1);
% 
% % load year axis
% years = popData(1,:);
% 
% % load data
% for regionNo = 2:dataSize
%     regionPops = popData(regionNo, :);
%     popFit(years, regionPops);
% end

popData = xlsread("data.xlsx", "gdp");
dataSize = size(popData, 1);

% load year axis
years = popData(1,:);

% load year axis
regionGdps = popData(42, :);
valid = 
gdpFit(years, regionGdps);